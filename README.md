# Pandas-DataFrames
# 🏀📊 Pandas DataFrame – Class Exercise (NBA Player Data)

This repository contains a Jupyter Notebook with a full set of exercises based on **Pandas DataFrames**, using an **NBA player dataset** to explore every core DataFrame operation — from indexing to cleaning to analysis.

## 📚 Topics Covered

### 🧱 **DataFrame Basics**

* Loading CSV data
* Exploring datasets using `head()`, `tail()`, `shape`, `info()`, `describe()`

### 🔍 **Indexing & Selection**

* Selecting rows & columns using `loc` & `iloc`
* Boolean filtering (e.g., players above certain height/points)
* Slicing subsets of rows
* Renaming columns
* Resetting/setting indices

### 🛠️ **Cleaning & Transforming**

* Handling missing values (`isna()`, `fillna()`, `dropna()`)
* Replacing incorrect or inconsistent values
* Removing duplicates
* Changing datatypes
* Sorting DataFrames
* Adding & dropping columns

### 🔢 **Math & Stats**

* Summary statistics (mean, median, std, etc.)
* Column-wise arithmetic
* Feature comparisons (heights, ages, salaries, points etc.)

### 🏀 **NBA Dataset Analysis**

* Basic exploratory insights
* Dataset-based mini tasks given in class

## 🎯 Key Learnings

This exercise helped me strengthen:

* Working efficiently with real-world sports data
* Cleaning and transforming DataFrames
* Using Pandas for exploratory data analysis (EDA)
* Applying vectorized operations for better performance
* Understanding structured datasets and statistical summaries

## 🚀 How to Run

1. Clone/download the repo
2. Open the `.ipynb` file in Jupyter Notebook, VS Code, or Google Colab
3. Run cells in order to view analysis and output
